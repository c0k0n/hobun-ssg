{
  "name": "hobun-ssg",
  "description": "Learning Hono and Bun by building a static site on Cloudflare Pages",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "NODE_ENV=development bun run --hot src/index.tsx",
    "build": "bun run scripts/build.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "preview": "wrangler pages dev dist"
  },
  "dependencies": {
    "hono": "^4.13.8"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "typescript": "^7.0.2",
    "wrangler": "^4.135.0"
  }
}